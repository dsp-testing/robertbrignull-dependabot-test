name: "CodeQL"

on:
  push:
  pull_request:
  schedule:
    - cron: '17 22 * * 6'

jobs:
  analyze:
    name: Analyze
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Initialize CodeQL
      uses: aeisenberg/codeql-action/init@aeisenberg/403-errors
      with:
        languages: javascript

    - name: Perform CodeQL Analysis
      uses: aeisenberg/codeql-action/analyze@aeisenberg/403-errors
